---
'@rockaway/css': minor
---

Add the focus ring and motion handling: one `:focus-visible` outline from the focus tokens, and reduced motion driven by the system setting or an in-app `data-motion="reduced"` attribute, collapsing the duration tokens and stopping animation already written in CSS.
