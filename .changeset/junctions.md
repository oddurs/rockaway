---
'@rockaway/grid': minor
---

Add the junction model: borders are weighted edges (none, light, heavy, double) and the glyph is looked up from them, so drawing order cannot change a seam. Includes the five border sets, with ASCII drawing the same geometry as `- | +`.
