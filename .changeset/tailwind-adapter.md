---
'@rockaway/tokens': minor
---

Generate a Tailwind v4 adapter, `@rockaway/tokens/tailwind.css`. Its `@theme inline` block maps the semantic tokens onto Tailwind's namespaces, so utilities resolve to the same custom properties the components read and follow the mode and density contexts.
