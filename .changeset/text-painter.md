---
'@rockaway/grid': minor
---

Add the text painter: `toText` renders a buffer as the characters it is, `fromText` reads one back, and `bufferSerializer` prints a buffer as its screen in snapshots, framed so width and trailing space are visible.
