---
'@rockaway/tokens': major
---

Replace the twelve-step ramps with the ANSI palette: the terminal's sixteen colours generated in OKLCH, plus the role slots a design system needs and a terminal does not name. The semantic tier keeps every name and now aliases `ansi.*`. Adds `importPalette` for reading a terminal theme in, and ships Ink, Phosphor and Ice beside the default.
