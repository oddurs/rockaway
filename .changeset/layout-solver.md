---
'@rockaway/grid': minor
---

Add the integer layout solver: fixed and growing tracks with weights, minimums and maximums, leftover cells distributed by largest remainder with ties going left, and overflow reported rather than hidden. `columns` and `rows` split a rect into rects.
