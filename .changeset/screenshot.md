---
'@rockaway/react': minor
---

Add `screenshot` at `@rockaway/react/testing`: reads a rendered screen back as the characters that are actually on it — painted chrome and real elements alike, in the cells they occupy — with the attributes listed underneath. Given a buffer instead, it is the buffer as text, so the same helper works in Node.
