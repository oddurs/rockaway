---
'@rockaway/css': minor
---

Add the reset and base layers. The reset normalises the platform; the base gives body, headings, links, code, rules, tables, placeholders and selection their defaults from semantic tokens, and declares a metric-matched Inter fallback so text does not shift when the font loads.
