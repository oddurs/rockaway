---
'@rockaway/tokens': minor
---

Add the semantic colour tier: `bg.*`, `fg.*` and `border.*`, each an alias to a palette step, written once for both modes. `bg.page` and `border.surface` follow the elevation input.
