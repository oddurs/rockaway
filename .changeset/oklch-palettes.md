---
'@rockaway/tokens': minor
---

Add OKLCH palettes: twelve role-based steps plus a `contrast` colour for neutral, accent, info, success, warning and danger, per mode. Export the colour maths (sRGB conversion, gamut mapping, WCAG contrast) the palettes are checked with.
