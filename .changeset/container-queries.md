---
'@rockaway/css': minor
---

Add the container-query convention: a named `rk` container, opted into with the `.rk-container` utility, with the width steps written down. Component CSS asks the container rather than the viewport.
