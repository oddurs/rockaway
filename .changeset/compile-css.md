---
'@rockaway/tokens': minor
---

Compile the tokens to CSS with Terrazzo: `@rockaway/tokens/tokens.css` declares every token as a `--rk-*` custom property in `@layer rk.tokens`, with sRGB-mapped colours and P3 and Rec. 2020 blocks. Export `vars`, a typed map from each DTCG path to its `var()`.
