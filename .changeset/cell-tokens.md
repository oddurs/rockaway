---
'@rockaway/tokens': major
'@rockaway/css': minor
---

Emit the cell: density is the line box (`dense`, `normal`, `airy`, `touch`), and space is a count of cells rather than a length — `ch` across, `lh` down, with the CSS layer turning a count into a length. Adds `size.screen.*` at 40, 60, 80 and 120 cells, and `conformance` to the theme inputs.
