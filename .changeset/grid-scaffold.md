---
'@rockaway/grid': minor
---

Add `@rockaway/grid`: the character-grid engine. Integer geometry, an immutable cell buffer with per-cell edge weights for the junction model, and styles that name semantic tokens rather than colours. No DOM, no dependencies.
