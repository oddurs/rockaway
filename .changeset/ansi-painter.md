---
'@rockaway/grid': minor
---

Add the ANSI painter: a screen as escape sequences at 16, 256 or truecolor depth, with attributes as SGR codes, runs coalesced, no line left with a style open, and `colorDepth` honouring `NO_COLOR`, `FORCE_COLOR` and what the terminal says about itself.
