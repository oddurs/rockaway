---
'@rockaway/grid': minor
---

Add drawing: boxes in any border set, titles set into the top edge, dividers and rules that join the walls they meet, text that respects wide characters, and `contentArea` for the space inside a border.
