---
'@rockaway/css': major
---

Rewrite the layers for the grid: `--rk-cell-width` (`1ch`) and `--rk-cell-height` (`1lh`) with every space count multiplied out as `--rk-x-*` and `--rk-y-*`, the monospace rhythm with ligatures off and one type size, state as attributes (`data-attrs="dim reverse"`), reverse-video focus on filled controls, forced colors mapped onto the ANSI roles, and container steps at 40, 60, 80 and 120 cells.
