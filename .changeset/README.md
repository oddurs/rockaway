# Changesets

Every change to a published package (`@rockaway/tokens`, `@rockaway/css`,
`@rockaway/react`) comes with a changeset: `pnpm changeset`.

Semver is strict. A changed semantic token is a minor release; a removed or
renamed one is major. See cairn item 0011.
