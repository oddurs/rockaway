---
'@rockaway/tokens': major
'@rockaway/css': patch
---

Add glyph and attribute tokens: the five border sets as characters keyed to the junction table, marks, blocks, the eight-step bar and the braille spinner, plus what bold, dim and underline mean in a theme. Type pairings are monospace families now — a proportional face cannot hold a character grid — and `borderSet` joins the theme inputs.
