---
'@rockaway/tokens': minor
---

Emit mode and density contexts in `tokens.css`. Dark mode follows `prefers-color-scheme` unless the root says `data-theme="light"`; `data-theme` and `data-density` work on any element, and they nest.
