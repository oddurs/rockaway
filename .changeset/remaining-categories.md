---
'@rockaway/tokens': minor
---

Complete the token categories: a type scale from 14px and a 1.2 ratio in rem, text styles by job (`text.body`, `text.heading-md`, `text.display-lg`…), radius derived from the control radius, `shadow.{control,surface,overlay}` with mode-aware tinted shadow colours and a scrim, `motion.duration.*` and `motion.easing.*`, and `focus.{width,offset}`. The card border now matches the concept (palette step 6 at L 0.905).
