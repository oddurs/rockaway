---
'@rockaway/grid': minor
---

Add text measurement in cells: `wcwidth` over Unicode 15 (2 for wide and fullwidth, 0 for combining marks and format characters), grapheme-aware slicing and truncation that never splits a character, wrapping that breaks on spaces where it can, tab expansion and padding.
